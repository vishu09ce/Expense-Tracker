/**
 * index.ts — Barrel export for all domain types.
 *
 * Import from '@/types' (or '../types') rather than individual files
 * to keep import paths short and refactoring easy.
 */

export * from './expense';
export * from './filters';
